apiVersion: v1
kind: Service
metadata:
  name: argocd-flask-demo
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
    type: NodePort
  selector:
    app: argocd-flask-demo
status:
  loadBalancer: {}
